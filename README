foobar-server

( ) Make the token generation more efficient by setting incremental index on the Client 
( ) Refactor FoobarService::sendNotificationToDevice().  Make the model more reusable.
( ) Add Push Notification in execRedeemPoints()
(x) Support Apple Push Notification
( ) Support Image Upload
( ) Return shop information on FBLoginShop and FBCreateShop
( ) Remove email and password information from FBGetShopInfo and GetShopListForDevice
( ) Create TransactionInfo for every add points and redeem points transaction
( ) Design a shop page
	( ) Create Customer List widget
	( ) Create Customer Transaction Widget
	( ) Create Shop Info page

[About Apple Push Notification]
Foobar uses Apple Push Notification system via Urban Airship.
Visit http://urbanairship.com for more information.
Login: yizumi@ripplesystem.com Password: ThepianX
